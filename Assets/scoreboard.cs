using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class scoreboard : MonoBehaviour
{
    /*
    [SerializeField] Text test;

    // Start is called before the first frame update
    void Start()
    {
        test.color = Color.white;
    }

    // Update is called once per frame
    void Update()
    {
        if (pointer.instance.number > 0 && pointer.instance.number < 8)
        {
            test.color = Color.red;
        }

        else if (pointer.instance.number == 0)
        {
            test.color = Color.green;
        }

        else
        {
            test.color = Color.black;
        }
    }

    void Test()
    {
        if (pointer.instance.number > 0 && pointer.instance.number < 8)
        {
            countdownText.fontStyle = FontStyle.Bold;
            cube.SetActive(true);
            cube.GetComponent<Material>().color = Color.yellow;
            if (currentTime <= 30.000f)
            {
                cube.SetActive(false);
            }
            return;
        }

        else if (pointer.instance.number == 0)
        {
            countdownText.fontStyle = FontStyle.Bold;
            cube.SetActive(true);
            cube.GetComponent<Material>().color = Color.yellow;

            if (currentTime <= 30.000f)
            {
                cube.SetActive(false);

            }
            return;
        }

        else if (pointer.instance.number > 7)
        {
            countdownText.fontStyle = FontStyle.Bold;
            cube.SetActive(true);
            cube.GetComponent<Material>().color = Color.yellow;
            if (currentTime <= 30.000f)
            {
                cube.SetActive(false);
            }
            return;
        }
    }*/
}
